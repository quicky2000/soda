#!/bin/sh

#Checking parameters
if [ $# -ne 1 ]
then echo "Usage is :  search <object_name> "
exit 3
fi

# Check if QUICKY_DP environment variable is set
if [ -z $QUICKY_DP ]
then 
version="dev"
else
version=`get_object_version $1`
fi
simple_search_object $1 $version


