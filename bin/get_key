#!/bin/sh

# Check if file existts
if [ ! -f $2 ]
then
echo "file $2 does not exists"
exit 1
fi

# get the line corresponding to the key
partial_result=`grep $1: $2`

# Check if key was found
if [ -z "$partial_result" ]
then 
echo "Key $1 not found in file $2"
exit 1
fi

# Extract key value
result=`echo $partial_result | cut -d":" -f2`
echo "$result"
